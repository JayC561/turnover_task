export enum ErrorMessages {
  Required = "Please enter a value",
  ValidEmail = "Not a valid email",
  StrongPassword = "Minimum 8 characters, 1 upper case letter, 1 lower case letter, 1 numeric digit",
}
